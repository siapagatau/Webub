<!DOCTYPE html>
<html>
<head>
  <title>📝 Daftar Akun</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #42b72a 0%, #1877f2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .card {
      background: white;
      border-radius: 20px;
      padding: 40px;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }
    .emoji-header {
      text-align: center;
      font-size: 4rem;
      margin-bottom: 20px;
    }
    h2 {
      text-align: center;
      color: #333;
      margin-bottom: 30px;
    }
    .form-group {
      margin-bottom: 20px;
    }
    label {
      display: block;
      margin-bottom: 5px;
      color: #666;
      font-size: 0.9rem;
    }
    input, textarea {
      width: 100%;
      padding: 12px 15px;
      border: 2px solid #e0e0e0;
      border-radius: 10px;
      font-size: 1rem;
      transition: border-color 0.3s;
    }
    input:focus, textarea:focus {
      outline: none;
      border-color: #42b72a;
    }
    textarea {
      resize: vertical;
      min-height: 80px;
    }
    button {
      width: 100%;
      padding: 12px;
      background: #42b72a;
      color: white;
      border: none;
      border-radius: 10px;
      font-size: 1rem;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s;
      margin-top: 10px;
    }
    button:hover {
      background: #3aa123;
    }
    .error {
      background: #fed7d7;
      color: #c53030;
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 20px;
      text-align: center;
    }
    .info {
      background: #e6f7ff;
      color: #0077b6;
      padding: 10px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 0.9rem;
    }
    .links {
      text-align: center;
      margin-top: 20px;
    }
    .links a {
      color: #42b72a;
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: bold;
    }
    .links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="emoji-header">📝</div>
    <h2>Daftar Akun Baru</h2>
    
    <div class="info">
      ℹ️ Password minimal 6 karakter
    </div>
    
    <% if (locals.error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    
    <form action="/register" method="POST">
      <div class="form-group">
        <label>👤 Username</label>
        <input type="text" name="username" placeholder="Pilih username" required autofocus>
      </div>
      
      <div class="form-group">
        <label>🔑 Password</label>
        <input type="password" name="password" placeholder="Minimal 6 karakter" required>
      </div>
      
      <div class="form-group">
        <label>🔑 Konfirmasi Password</label>
        <input type="password" name="confirmPassword" placeholder="Ketik ulang password" required>
      </div>
      
      <div class="form-group">
        <label>📝 Bio (opsional)</label>
        <textarea name="bio" placeholder="Ceritakan tentang dirimu..."></textarea>
      </div>
      
      <button type="submit">Daftar</button>
    </form>
    
    <div class="links">
      <a href="/login">🔐 Sudah punya akun? Login</a>
      <br>
      <a href="/">🏠 Kembali ke Beranda</a>
    </div>
  </div>
</body>
</html>